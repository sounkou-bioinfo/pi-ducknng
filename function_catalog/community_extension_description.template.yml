extension:
  name: ${extension_name}
  description: ${extension_description}
  version: ${extension_version}
  language: ${extension_language}
  build: ${extension_build}
  license: ${extension_license}
${optional_extension_fields}  maintainers:
${maintainers_block}

repo:
  github: ${repo_github}
  ref: ${repo_ref}

docs:
  hello_world: |
${hello_world_block}
  extended_description: |
${extended_description_block}
