diff --git a/third_party/nng/src/core/aio.c b/third_party/nng/src/core/aio.c
index 40a84b1..2a3f69c 100644
--- a/third_party/nng/src/core/aio.c
+++ b/third_party/nng/src/core/aio.c
@@ -13,0 +14,23 @@
+#if defined(__EMSCRIPTEN__)
+#include <stdio.h>
+#endif
+
+#if defined(__EMSCRIPTEN__) && defined(DUCKNNG_WASM_TRACE) && DUCKNNG_WASM_TRACE
+static void
+ducknng_nng_wasm_trace(const char *where)
+{
+	fprintf(stderr, "[ducknng nng wasm trace] %s\n", where ? where : "(null)");
+	fflush(stderr);
+}
+#else
+static void
+ducknng_nng_wasm_trace(const char *where)
+{
+	NNI_ARG_UNUSED(where);
+#if defined(__EMSCRIPTEN__)
+	__asm__ __volatile__("" ::: "memory");
+	fflush(stderr);
+#endif
+}
+#endif
+
@@ -587,0 +611 @@ nni_aio_expire_loop(void *arg)
+	ducknng_nng_wasm_trace("aio: expire worker entered");
@@ -813,0 +838 @@ nni_aio_expire_q_alloc(void)
+	ducknng_nng_wasm_trace("aio: expire thread init begin");
@@ -818,0 +844 @@ nni_aio_expire_q_alloc(void)
+	ducknng_nng_wasm_trace("aio: expire thread run begin");
@@ -848,0 +875 @@ nni_aio_sys_init(void)
+	ducknng_nng_wasm_trace("aio: sysinit begin");
@@ -866,0 +894 @@ nni_aio_sys_init(void)
+		ducknng_nng_wasm_trace("aio: expire queue alloc begin");
@@ -873,0 +902 @@ nni_aio_sys_init(void)
+	ducknng_nng_wasm_trace("aio: sysinit returned");
diff --git a/third_party/nng/src/core/init.c b/third_party/nng/src/core/init.c
index c7a660b..99eb841 100644
--- a/third_party/nng/src/core/init.c
+++ b/third_party/nng/src/core/init.c
@@ -17,0 +18,19 @@
+#if defined(__EMSCRIPTEN__) && defined(DUCKNNG_WASM_TRACE) && DUCKNNG_WASM_TRACE
+static void
+ducknng_nng_wasm_trace(const char *where)
+{
+	fprintf(stderr, "[ducknng nng wasm trace] %s\n", where ? where : "(null)");
+	fflush(stderr);
+}
+#else
+static void
+ducknng_nng_wasm_trace(const char *where)
+{
+	NNI_ARG_UNUSED(where);
+#if defined(__EMSCRIPTEN__)
+	__asm__ __volatile__("" ::: "memory");
+	fflush(stderr);
+#endif
+}
+#endif
+
@@ -35,0 +55 @@ nni_init_helper(void)
+	ducknng_nng_wasm_trace("init: taskq sysinit begin");
@@ -37,3 +57,3 @@ nni_init_helper(void)
-	    ((rv = nni_reap_sys_init()) != 0) ||
-	    ((rv = nni_aio_sys_init()) != 0) ||
-	    ((rv = nni_tls_sys_init()) != 0)) {
+	    (ducknng_nng_wasm_trace("init: reap sysinit begin"), (rv = nni_reap_sys_init()) != 0) ||
+	    (ducknng_nng_wasm_trace("init: aio sysinit begin"), (rv = nni_aio_sys_init()) != 0) ||
+	    (ducknng_nng_wasm_trace("init: tls sysinit begin"), (rv = nni_tls_sys_init()) != 0)) {
@@ -44,0 +65 @@ nni_init_helper(void)
+	ducknng_nng_wasm_trace("init: transport sysinit begin");
@@ -57,0 +79 @@ nni_init(void)
+	ducknng_nng_wasm_trace("init: nni_init begin");
diff --git a/third_party/nng/src/core/reap.c b/third_party/nng/src/core/reap.c
index 3f18220..708f89f 100644
--- a/third_party/nng/src/core/reap.c
+++ b/third_party/nng/src/core/reap.c
@@ -16,0 +17,23 @@
+#if defined(__EMSCRIPTEN__)
+#include <stdio.h>
+#endif
+
+#if defined(__EMSCRIPTEN__) && defined(DUCKNNG_WASM_TRACE) && DUCKNNG_WASM_TRACE
+static void
+ducknng_nng_wasm_trace(const char *where)
+{
+	fprintf(stderr, "[ducknng nng wasm trace] %s\n", where ? where : "(null)");
+	fflush(stderr);
+}
+#else
+static void
+ducknng_nng_wasm_trace(const char *where)
+{
+	NNI_ARG_UNUSED(where);
+#if defined(__EMSCRIPTEN__)
+	__asm__ __volatile__("" ::: "memory");
+	fflush(stderr);
+#endif
+}
+#endif
+
@@ -30,0 +54 @@ reap_worker(void *unused)
+	ducknng_nng_wasm_trace("reap: worker entered");
@@ -109,0 +134 @@ nni_reap_sys_init(void)
+	ducknng_nng_wasm_trace("reap: thread init begin");
@@ -112,0 +138 @@ nni_reap_sys_init(void)
+	ducknng_nng_wasm_trace("reap: thread run begin");
diff --git a/third_party/nng/src/core/taskq.c b/third_party/nng/src/core/taskq.c
index 0988659..9b6ccc1 100644
--- a/third_party/nng/src/core/taskq.c
+++ b/third_party/nng/src/core/taskq.c
@@ -12,0 +13,23 @@
+#if defined(__EMSCRIPTEN__)
+#include <stdio.h>
+#endif
+
+#if defined(__EMSCRIPTEN__) && defined(DUCKNNG_WASM_TRACE) && DUCKNNG_WASM_TRACE
+static void
+ducknng_nng_wasm_trace(const char *where)
+{
+	fprintf(stderr, "[ducknng nng wasm trace] %s\n", where ? where : "(null)");
+	fflush(stderr);
+}
+#else
+static void
+ducknng_nng_wasm_trace(const char *where)
+{
+	NNI_ARG_UNUSED(where);
+#if defined(__EMSCRIPTEN__)
+	__asm__ __volatile__("" ::: "memory");
+	fflush(stderr);
+#endif
+}
+#endif
+
@@ -37,0 +61 @@ nni_taskq_thread(void *self)
+	ducknng_nng_wasm_trace("taskq: worker entered");
@@ -73,0 +98 @@ nni_taskq_init(nni_taskq **tqp, int nthr)
+	ducknng_nng_wasm_trace("taskq: init begin");
@@ -91,0 +117 @@ nni_taskq_init(nni_taskq **tqp, int nthr)
+		ducknng_nng_wasm_trace("taskq: worker init begin");
@@ -100,0 +127 @@ nni_taskq_init(nni_taskq **tqp, int nthr)
+		ducknng_nng_wasm_trace("taskq: worker run begin");
@@ -103,0 +131 @@ nni_taskq_init(nni_taskq **tqp, int nthr)
+	ducknng_nng_wasm_trace("taskq: init returned");
@@ -272,0 +301 @@ nni_taskq_sys_init(void)
+	ducknng_nng_wasm_trace("taskq: sysinit begin");
diff --git a/third_party/nng/src/platform/posix/posix_thread.c b/third_party/nng/src/platform/posix/posix_thread.c
index b7cd5e1..e76b5bc 100644
--- a/third_party/nng/src/platform/posix/posix_thread.c
+++ b/third_party/nng/src/platform/posix/posix_thread.c
@@ -26,0 +27,38 @@
+#if defined(__EMSCRIPTEN__) && defined(DUCKNNG_WASM_INPROC_ONLY) && DUCKNNG_WASM_INPROC_ONLY
+#define DUCKNNG_NNG_WASM_INPROC_ONLY 1
+#else
+#define DUCKNNG_NNG_WASM_INPROC_ONLY 0
+#endif
+
+#if defined(__EMSCRIPTEN__)
+#include <emscripten/threading.h>
+#include <stdio.h>
+#endif
+
+static void
+ducknng_nng_wasm_thread_yield(void)
+{
+#if defined(__EMSCRIPTEN__) && defined(DUCKNNG_WASM_INPROC_ONLY) && DUCKNNG_WASM_INPROC_ONLY
+	emscripten_thread_sleep(1.0);
+#endif
+}
+
+#if defined(__EMSCRIPTEN__) && defined(DUCKNNG_WASM_TRACE) && DUCKNNG_WASM_TRACE
+static void
+ducknng_nng_wasm_trace(const char *where)
+{
+	fprintf(stderr, "[ducknng nng wasm trace] %s\n", where ? where : "(null)");
+	fflush(stderr);
+}
+#else
+static void
+ducknng_nng_wasm_trace(const char *where)
+{
+	NNI_ARG_UNUSED(where);
+#if defined(__EMSCRIPTEN__)
+	__asm__ __volatile__("" ::: "memory");
+	fflush(stderr);
+#endif
+}
+#endif
+
@@ -278,0 +317,2 @@ nni_plat_thr_init(nni_plat_thr *thr, void (*fn)(void *), void *arg)
+	ducknng_nng_wasm_thread_yield();
+	ducknng_nng_wasm_trace("posix_thread: pthread_create begin");
@@ -279,0 +320 @@ nni_plat_thr_init(nni_plat_thr *thr, void (*fn)(void *), void *arg)
+	ducknng_nng_wasm_thread_yield();
@@ -280,0 +322 @@ nni_plat_thr_init(nni_plat_thr *thr, void (*fn)(void *), void *arg)
+		ducknng_nng_wasm_trace("posix_thread: pthread_create failed");
@@ -284,0 +327 @@ nni_plat_thr_init(nni_plat_thr *thr, void (*fn)(void *), void *arg)
+	ducknng_nng_wasm_trace("posix_thread: pthread_create returned");
@@ -395,0 +439,4 @@ nni_plat_init(int (*helper)(void))
+#if DUCKNNG_NNG_WASM_INPROC_ONLY
+	ducknng_nng_wasm_trace("posix_thread: skip pollq/resolver sysinit for wasm inproc-only");
+#else
+	ducknng_nng_wasm_trace("posix_thread: pollq sysinit begin");
@@ -403,0 +451 @@ nni_plat_init(int (*helper)(void))
+	ducknng_nng_wasm_trace("posix_thread: resolver sysinit begin");
@@ -411,0 +460 @@ nni_plat_init(int (*helper)(void))
+#endif
@@ -412,0 +462,4 @@ nni_plat_init(int (*helper)(void))
+#if DUCKNNG_NNG_WASM_INPROC_ONLY
+	ducknng_nng_wasm_trace("posix_thread: skip atfork for wasm inproc-only");
+#else
+	ducknng_nng_wasm_trace("posix_thread: atfork begin");
@@ -421,0 +475,2 @@ nni_plat_init(int (*helper)(void))
+#endif
+	ducknng_nng_wasm_trace("posix_thread: helper begin");
@@ -424,0 +480 @@ nni_plat_init(int (*helper)(void))
+	ducknng_nng_wasm_trace("posix_thread: helper returned");
@@ -434,0 +491 @@ nni_plat_fini(void)
+#if !DUCKNNG_NNG_WASM_INPROC_ONLY
@@ -436,0 +494 @@ nni_plat_fini(void)
+#endif
